{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row" id="row_title">
            <div class="title">
                <h2>{{ film.titre }}</h2>
            </div>
        </div>
        <div class="row">
            <div class="content col-md-6">
                <img src="https://image.tmdb.org/t/p/w500{{ film.image }}" alt="">
            </div>
            <div class="col-md-6">
                    {{ film.description }}
                <p>
                    <h5>Distribution, production et réalisation</h5>
                </p>
                {% for personne in personnes %}
                    <a href="{{ path('person_show', {'id': personne.id}) }}">
                        {{ personne.prenom }}
                        {{ personne.nom }}
                {% endfor %}
                </div>
                <div class="col-md-6"></div>
            </div>
        {% endblock %}